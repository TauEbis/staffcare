<%= render 'header' %>

<% @zone.locations.each do |location| %>
  <%= render 'basic', location_plan: location %>
<% end %>

<%= link_to 'Edit', edit_schedule_path(@schedule) %> |
<%= link_to 'Back', schedules_path %>
